import { css, theme } from '@sam/theme/twind';

export const EntryModalCss = css({
  '&': {
    display: 'flex',
    flexFlow: 'column',
    alignItems: 'flex-end',
    gap: theme('spacing.8'),
  },
});
